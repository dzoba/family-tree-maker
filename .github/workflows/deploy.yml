name: Deploy to Firebase

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - run: npm run build

      - run: cd functions && npm ci

      - run: npx firebase-tools deploy --force --project family-tree-maker-2e1f5
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
